apiVersion: v1
kind: Service
metadata:
  labels:
    app: python-flask-sample-app
  name: python-flask-sample-app
spec:
  ports:
  - name: 5000-5000
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: python-flask-sample-app
  type: ClusterIP
